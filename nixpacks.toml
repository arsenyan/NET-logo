# Keep build-time tools available. Railpack/Nixpacks otherwise skip the
# nested parcel workspace and fail `yarn run build` with exit 127.
[variables]
YARN_PRODUCTION = "false"
NPM_CONFIG_PRODUCTION = "false"
